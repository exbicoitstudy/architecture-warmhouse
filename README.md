# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование

<aside>

Чтобы составить документ с описанием текущей архитектуры приложения, можно часть информации взять из описания компании и условия задания. Это нормально.

</aside>

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- Пользователи могут удалённо управлять отоплением в своих домах.
- Система поддерживает подключение датчиков только с помощью специалистов.

**Мониторинг температуры:**

- Система получает данные о температуре с датчиков, установленных в домах.
- Пользователи могут просматривать текущую температуру в своих домах через веб-интерфейс.

### 2. Анализ архитектуры монолитного приложения

**Язык программирования**: Go

**База данных**: PostgreSQL

**Архитектура**: Монолитная, все компоненты системы (обработка запросов, бизнес-логика, работа с данными) находятся в рамках одного приложения.

**Взаимодействие**: Синхронное, запросы обрабатываются последовательно.

**Масштабируемость**: Ограничена, так как монолит сложно масштабировать по частям.

**Развертывание**: Требует остановки всего приложения.

### 3. Определение доменов и границы контекстов

**Домен**: Управление устройствами

Поддомен: Управление отоплением
- Контекст: Команды включения/выключения отопления

Поддомен: Мониторинг температуры
- Контекст: Сбор и отображение данных о текущей температуре

**Домен**: Пользователи

Поддомен: Профиль
- Контекст: Хранение и управление данными пользователей и их устройствами


### **4. Проблемы монолитного решения**

- Ограниченная масштабируемость.
  Все функции (управление отоплением, телеметрия) находятся в одном модуле. Для увеличения производительности приходится масштабировать весь монолит.

- Жёсткая связь компонентов.
  Изменения в одном блоке могут повлиять на другие из-за тесной связи между частями системы.

- Длительный цикл выпуска.
  Любые обновления требуют пересборки и перезапуска всего приложения.

- Отсутствие self-service для пользователя.
  Добавление новых устройств возможно только через выезд специалиста — пользователь не может сделать это самостоятельно.

- Трудности с тестированием и развёртыванием.
  Из-за монолитности сложно изолировать отдельные бизнес-функции для тестов. Автоматизация CI/CD осложнена необходимостью прогонять полный набор тестов при любой правке.

- Единая база данных.
  Все доменные данные и телеметрия хранятся в одной PostgreSQL — нет возможности разграничить нагрузку.

- Ограниченная гибкость расширения.
  Добавление новых модулей (освещение, ворота, видео-мониторинг) потребует внесения изменений в ядро монолита, что ведёт к усложнению кода и росту зависимости между функциональными частями.

- Низкая отказоустойчивость.
  Падение любого подмодуля приводит к недоступности всей системы целиком, вместо изоляции проблемы внутри одного микросервиса.

- Невозможность приоритизации ресурсов.
  Нет возможности выделить отдельные контейнеры под часто используемые и редко используемые функции, чтобы оптимизировать использование CPU, памяти и сетевых ресурсов.


### 5. Визуализация контекста системы — диаграмма С4

[Текущая реализация диаграмма контекста](./diagrams/current.puml)

# Задание 2. Проектирование микросервисной архитектуры

В этом задании вам нужно предоставить только диаграммы в модели C4. Мы не просим вас отдельно описывать получившиеся микросервисы и то, как вы определили взаимодействия между компонентами To-Be системы. Если вы правильно подготовите диаграммы C4, они и так это покажут.

**Диаграмма контейнеров (Containers)**

[Новая диаграма контейнеров](./diagrams/new-containers.puml)

**Диаграмма компонентов (Components)**

[Новая диаграма компонентов (profile)](./diagrams/profile-microservise-components.puml)

[Новая диаграма компонентов (devices)](./diagrams/devices-microservise-components.puml)

[Новая диаграма компонентов (monitoring)](./diagrams/monitoring-microservise-components.puml)

[Новая диаграма компонентов (triggers)](./diagrams/triggers-microservise-components.puml)

**Диаграмма кода (Code)**

Мы уже покрываем архитектуру на уровнях контекста, контейнеров и компонентов.
Уровень кода будет меняться в процессе реализации MVP, поэтому фиксировать его заранее — нецелесообразно: диаграмма быстро устареет и потеряет актуальность. 
Более того, слишком ранняя детализация кода может помешать гибкости на этапе активной разработки.

В качестве примера диаграмма кода для добавления устройства в сервисе устройств
[Диаграмма кода (devices)](./diagrams/add-device-code.puml)

# Задание 3. Разработка ER-диаграммы

[ER-диаграмма](./diagrams/bd.puml)

# Задание 4. Создание и документирование API

### 1. Тип API

Для взаимодействия микросервисов можно использовать **Синхронный REST** и **Асинхронный AsyncAPI**, выбор зависит от специфики взаимодействия.
Для операций, где вызывающий сервис ждёт немедленного подтверждения выполнения, следует использовать синхронный REST, а для событий, которые генерируются сервисами и обрабатываются подписчиками, подходит асинхронный AsyncAPI.

В текущем проекте будем использовать оба типа апи.

### 2. Документация API

[REST API сервиса управления устройствами](apps/docs/device/swagger.yaml)

[REST API сервиса мониторинга](apps/docs/monitoring/swagger.yaml)

[REST API сервиса профиля](apps/docs/profile/swagger.yaml)

[REST API сервиса триггеров](apps/docs/trigger/swagger.yaml)

[ASYNC API (брокер сообщений)](apps/docs/monitoring/asyncapi.yaml)


# Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

1) сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

```
	// If no location is provided, use a default based on sensor ID
	if location == "" {
		switch sensorID {
		case "1":
			location = "Living Room"
		case "2":
			location = "Bedroom"
		case "3":
			location = "Kitchen"
		default:
			location = "Unknown"
		}
	}

	// If no sensor ID is provided, generate one based on location
	if sensorID == "" {
		switch location {
		case "Living Room":
			sensorID = "1"
		case "Bedroom":
			sensorID = "2"
		case "Kitchen":
			sensorID = "3"
		default:
			sensorID = "0"
		}
	}
```

2) Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

3) Кроме того для smart_home приложения требуется база данных - добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

- Create Sensor
- Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.


